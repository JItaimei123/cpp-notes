## 一、引用的特性

```C
#include <iostream>
using namespace std;

int main() {
    int a = 10;
    // int &ra;        // ❌ 错误：引用必须在定义时初始化
    int &b = a;        // ✅ b 是 a 的引用
    int c = 20;
    b = c;             // 这不是让 b 引用 c，而是把 c 的值赋给 b（即 a = c）
    cout << &a << endl; // 地址
    cout << &b << endl; // 地址与 a 相同
    cout << &c << endl; // 地址与 a 不同
    return 0;
}
```


### 关键点：

- **定义时必须初始化**，不能先声明再赋值。

- **一个变量可以有多个引用**（多个别名）。

- **引用一旦绑定，不能改变指向**（不能让它成为另一个变量的引用）。

---

## 二、引用的主要使用场景

### 1. 引用传参（替代指针，简化代码）

```C
void Swap(int &rx, int &ry) {
    int tmp = rx;
    rx = ry;
    ry = tmp;
}

int main() {
    int x = 0, y = 1;
    cout << x << " " << y << endl; // 0 1
    Swap(x, y);
    cout << x << " " << y << endl; // 1 0
    return 0;
}
```


**优点**：语法简洁，不需要取地址和解引用，直接修改变量。

---

### 2. 引用做返回值（可修改返回的对象）

```C
int& STTop(ST& rs) {    // 返回栈顶元素的引用
    assert(rs.top > 0);
    return rs.a[rs.top - 1];  // 注意：这里应返回栈顶元素下标（根据上下文）
}

int main() {
    ST st1;
    STInit(st1);
    STPush(st1, 1);
    STPush(st1, 2);
    cout << STTop(st1) << endl; // 2
    STTop(st1) += 10;           // 直接修改栈顶元素
    cout << STTop(st1) << endl; // 12
    return 0;
}
```


**注意**：返回引用时，不能返回局部变量的引用（因为局部变量在函数结束时销毁）。通常用于返回成员变量或静态变量。

---

## 三、引用与指针的对比

|特性|指针|引用|
|-|-|-|
|是否可为空|可以 `nullptr`|必须初始化，不能为空|
|能否改变指向|可以|不能|
|语法|需要 `*` 解引用|直接使用，如变量|
|传参|需要传地址 `&var`|直接传变量，自动引用|
|典型用途|动态内存、链表结点|函数参数、返回值、别名|

> 实践中，引用和指针相辅相成，不可完全替代。

---

## 四、链表头插中的引用（简化二级指针）

### 传统 C 写法（需要二级指针）

```C
void ListPushBack(LTNode** phead, int x) {
    LTNode* newnode = (LTNode*)malloc(sizeof(LTNode));
    newnode->val = x;
    newnode->next = NULL;
    if (*phead == NULL) {
        *phead = newnode;
    } else {
        // 找尾...
    }
}
int main() {
    LTNode* plist = NULL;
    ListPushBack(&plist, 1);
    return 0;
}
```


### C++ 引用写法（简化）

```C
// 定义指针别名
typedef struct ListNode 
{
    int val;
    struct ListNode* next;
} LTNode, *PNode;//LTNode是类型别名

// 使用指针的引用
void ListPushBack(PNode& phead, int x) 
  //等价于LTNode *&phead，若不用传引用调用则要用到双指针，PNode**plist
{
    PNode newnode = (PNode)malloc(sizeof(LTNode));
    newnode->val = x;
    newnode->next = NULL;
    if (phead == NULL) {
        phead = newnode;   // 直接修改外部的 phead
    } else {
        // 找尾...
    }
}

int main() {
    PNode plist = NULL;
    ListPushBack(plist, 1);  
  // 这里不需要取地址，在这里调用函数，plist的别名就是第8行中的phead，这里是传引用调用
    return 0;
}
```


**解释**：

对于以下代码

```C
typedef struct ListNode 
{
    int val;
    struct ListNode* next;
} LTNode, *PNode;
```


- `struct ListNode` 是结构体类型的**完整名字**（标签为 `ListNode`）。

- `LTNode` 是 `struct ListNode` 的结构体类型**别名**，以后可以直接用 `LTNode` 定义变量，而不用写 `struct ListNode`。

- `PNode` 是 `struct ListNode*` 的"指针类型"的**别名**（指向该结构体的指针类型）`*PNode` 中的 `*` 不是解引用，而是 **类型声明的一部分**，表示 `PNode` 是一个指针类型。这种写法是 `typedef` 允许的，目的是简化指针类型的声明。

- `LTNode`是类型名字，`PNode`就是`LTNode`的指针



```C
void ListPushBack(PNode& phead, int x) 
```


- `PNode& phead` 是指针的引用，它直接绑定到实参指针 `plist`。

- 对 `phead` 的修改就是对 `plist` 的修改，无需二级指针。

等价于void ListPushBack(LTNode*& phead, int x);（传引用）

但是若是想等价于void ListPushBack(LTNode** plist, int x);那就要在实参那里传地址

```C++
ListPushBack(plist, 1); //改成下面那种传地址才能用二级指针
 ListPushBack(&plist, 1);  
```




---

## 五、数据结构中使用引用传参（以栈为例）

```C
#include <iostream>
#include <cassert>
#include <cstdlib>
using namespace std;

typedef int STDataType;
typedef struct Stack {
    STDataType* a;
    int top;
    int capacity;
} ST;

// 初始化：使用引用，避免二级指针
void STInit(ST& rs, int n = 4) {
    rs.a = (STDataType*)malloc(n * sizeof(STDataType));
    rs.top = 0;
    rs.capacity = n;
}

// 入栈
void STPush(ST& rs, STDataType x) {
    if (rs.top == rs.capacity) {
        int newcapacity = rs.capacity == 0 ? 4 : rs.capacity * 2;
        STDataType* tmp = (STDataType*)realloc(rs.a, newcapacity * sizeof(STDataType));
        if (tmp == NULL) {
            perror("realloc fail");
            return;
        }
        rs.a = tmp;
        rs.capacity = newcapacity;
    }
    rs.a[rs.top++] = x;
}

// 返回栈顶元素（引用版本，允许修改）
int& STTop(ST& rs) {
    assert(rs.top > 0);
    return rs.a[rs.top - 1];
}

int main() {
    ST st1;
    STInit(st1);
    STPush(st1, 1);
    STPush(st1, 2);
    cout << STTop(st1) << endl;   // 2
    STTop(st1) += 10;             // 通过引用修改栈顶
    cout << STTop(st1) << endl;   // 12
    return 0;
}
```


**要点**：

- 使用引用代替指针传参，使代码更简洁。

- 返回引用可以让调用者直接修改栈顶元素。

---

## 六、注意事项

1. **引用不能绑定到临时对象**（除非是 `const` 引用，但这里不展开）。

2. **不要返回局部变量的引用**（生命周期结束会销毁）。

3. **在数据结构中，用引用传参可以替代一级指针和二级指针**，但需谨慎使用，确保引用的生命周期有效

4. 权限放大只在指针和引用里面会有，在拷贝中没有。

![image.png](https://cdn2.flowus.cn/oss/2f84482e-f789-417b-a828-8653a597390d/image.png?time=1782974700&token=7232834d82ee3f5658bacb2d577e6b2d62755118cae4e20d42bd4e1c91ecc3a0&role=sharePaid)

第一个是经典的权限放大，第二个就不是权限放大，只是一个拷贝操作。

![image.png](https://cdn2.flowus.cn/oss/99647882-471a-42a2-b2c9-d4d2d18c3dee/image.png?time=1782974700&token=c24340ffd493c27998bd489d44af6aa62b3962b4237ddf76bb2b2f37011e1aa1&role=sharePaid)

在这种情况下如果const int &ri = d；没有const就是错的，在前两步将d的值赋给i涉及隐式类型转换，也会产生临时对象，临时对象具有常性，本质也是拷贝，这个临时空间也是只读不写的，所以不加const的话就是权限放大了

## 临时对象产生的几种常见情况（C++）

### 1. 隐式类型转换

当将一种类型赋值给另一种类型（需要转换）时，编译器会创建一个临时对象。

```C
double d = 12.34;
int i = d;              // double -> int，产生临时 int（右值）
const int& ri = d;      // double -> int，产生临时 int，常量引用可绑定
// int& r = d;          // 错误：非常量引用不能绑定到临时对象
```


### 2. 按值传递参数

函数参数按值传递时，实参被拷贝给形参，这个拷贝过程会创建临时对象（若参数是类类型则调用拷贝构造，内置类型是直接复制）。

```C
void func(int x) {}   // x 是实参的副本
int main() {
    int a = 10;
    func(a);          // 不产生明显的临时（a 直接复制给 x，对内置类型编译器优化）
    // 对于类类型，会调用拷贝构造函数产生临时形参
}
```


### 3. 函数按值返回

函数返回一个对象时，通常会创建一个临时对象（返回值），在调用处接收。

```C
int add(int a, int b) {
    return a + b;     // 产生临时 int，保存 a+b 的结果
}
int main() {
    int res = add(3, 5);   // add 返回的临时对象用于初始化 res
}
```


### 4. 表达式计算中的中间结果

算术运算、关系运算等会产生临时对象。

```C
int a = 3, b = 5, c = 7;
int d = a + b * c;    // b * c 产生临时 int，然后与 a 相加再产生另一个临时 int
```


### 5. 类型转换操作（显式或隐式）

除了隐式转换，使用 `static_cast` 等也会产生临时对象。

```C
double d = 3.14;
int i = static_cast<int>(d);   // 产生临时 int
```


### 6. 返回临时对象的引用（需注意生命周期）

```C
const int& getTemp() {
    return 42;        // 字面量 42 产生临时 int，返回其引用（危险，但常量引用可延长临时生命周期？实际上函数返回时临时对象已销毁）
}
// 正确做法：按值返回
```


### 7. 类类型：函数返回值、拷贝构造、运算符重载

```C
struct A {
    A() {}
    A(const A&) {}
    A operator+(const A&) const { return A(); }
};
A a, b;
A c = a + b;    // a+b 产生临时对象，然后拷贝构造 c（可能有优化：编译器可能省略临时）
```


---

## 代码演示（完整示例）

```C
#include <iostream>
using namespace std;

struct Test {
    Test() { cout << "构造\n"; }
    Test(const Test&) { cout << "拷贝构造\n"; }
    ~Test() { cout << "析构\n"; }
};

Test create() {
    return Test();      // 返回临时对象，可能被优化（RVO）
}

void accept(Test t) {   // 按值传递，实参拷贝给形参
}

int main() {
    // 1. 隐式转换
    double d = 3.14;
    int i = d;          // double -> int 产生临时 int
    cout << i << endl;

    // 2. 表达式临时
    int x = 10, y = 20;
    int z = x + y;      // x+y 产生临时 int

    // 3. 类类型临时
    Test t1;
    accept(t1);         // 拷贝构造临时形参
    Test t2 = create(); // 可能产生临时，但编译器优化（RVO）可能省略

    // 4. const 引用绑定到临时
    const int& r = 42;  // 绑定到字面量产生的临时 int，生命周期延长
    cout << r << endl;

    return 0;
}
```


---

## 要点总结（供笔记）

|情况|说明|是否产生临时对象|
|-|-|-|
|隐式类型转换|如 `double` → `int`|✅ 产生临时右值|
|按值传递参数|实参拷贝给形参|✅ 对于类类型产生临时（内置类型可能优化）|
|按值返回|返回对象时|✅ 可能产生临时（受RVO影响）|
|表达式中间结果|如 `a + b`|✅ 产生临时|
|类型转换操作|`static_cast<int>(d)`|✅ 产生临时|
|常量引用绑定到右值|`const T& r = T()`|✅ 临时生命周期延长|
|非常量引用绑定到右值|❌ 编译错误|不允许|

**关键规则**：非常量左值引用不能绑定到临时对象（右值）。临时对象是右值，只能绑定到 `const` 左值引用或右值引用。