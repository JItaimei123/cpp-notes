# 一.哈希概念

## 1.直接定址法

#### **直接定址法就是用键的值（或键的简单变换）作为它在数组中的位置，实现 O(1) 级别的快速存取。**

---

### 两种形式

|形式|公式|说明|适用场景|
|-|-|-|-|
|**一对一映射**|`地址 = 键的值`|直接用键作为下标|键的数值范围较小且连续|
|**线性映射**|`地址 = a * 键 + b`|通过简单线性变换得到下标|键有规律，但需要调整偏移量|

---

### 代码中的例子（一对一映射）

```C++
int count[26] = {0};
for (char &c : s) {
    count[c - 'a']++;  // 地址 = 键 - 'a'
}
```


|字符 `c`|`c - 'a'`（地址）|
|-|-|
|`'a'`|0|
|`'b'`|1|
|`'c'`|2|
|...|...|
|`'z'`|25|

这里，哈希函数就是 `c - 'a'`，把字符 `'a'` 映射到下标 `0`，`'b'` 映射到 `1`，以此类推。这是一个**完美的一一对应**，没有冲突。

---

### 另一个例子（线性映射）

如果键是 ASCII 码，但你想从 `0` 开始存：

```C++
// 假设要统计 ASCII 码 32 ~ 126 的字符出现次数
int count[95] = {0};
for (char &c : s) {
    count[c - 32]++;  // 线性映射：地址 = 键 - 32
}
```


|ASCII 码|地址 = ASCII - 32|
|-|-|
|32（空格）|0|
|65（'A'）|33|
|122（'z'）|90|

---

### 优缺点分析

|优点|缺点|
|-|-|
|**简单高效**：计算速度快，没有复杂的哈希函数|**空间浪费严重**：如果键的范围很大（如 `10^9`），需要分配巨大的数组|
|**无冲突**：直接映射，无需处理哈希冲突|**键必须连续或可预测**：不适用于非整数、范围不连续的键|
|**O(1) 时间复杂度**：查找和修改都是常数时间|**无法处理动态扩展**：键的范围需要提前确定|

---

### 什么时候用直接定址法？

|适用场景|不适用场景|
|-|-|
|键的范围**小且连续的**（如 26 个英文字母）|键的范围**巨大**（如 64 位整数）|
|键是**固定有限集合**（如 0~255 的 ASCII 码）|键是**字符串**或**复杂对象**|
|需要**极致性能**，且不介意空间浪费|需要**动态扩展**，或者键的类型不固定|

## 2.哈希冲突

哈希冲突（Hash Collision），就是**两个不同的输入值，经过同一个哈希函数计算后，得到了相同的输出值（索引或地址）**。

## 3.负载因子

**负载因子**是衡量哈希表"装满程度"的一个指标，也是决定哈希表**何时扩容**的关键参数。

**负载因子 = 哈希表中已有元素个数 / 哈希表的总槽位数**

### 负载因子与性能的关系

|负载因子|含义|性能影响|
|-|-|-|
|**较小（如 0.5）**|表很空，很多槽位空闲|冲突少，查找/插入快，但**浪费内存，空间利用率低**|
|**较大（如 0.9）**|表快满了，槽位很紧张|冲突多，查找/插入变慢，但**节省内存，空间利用率高**|
|**0.75（默认值）**|平衡点|性能和空间的折中，STL 的默认值|

负载因子越高，哈希冲突越严重，哈希表的性能会从 O(1) 向 O(n) 退化。

# 二.哈希函数

## 1.除留余数法

**定义**：哈希函数为 `hash(key) = key % p`，其中 `p` 是一个**不大于哈希表长度 `m` 的质数**。

它的核心逻辑是：用键 `key` 除以一个质数 `p`，把**余数**作为它在数组中的存储位置。

```C++
int hash(int key) {
    return key % p;  // p 是质数，例如 7
}
```


### 为什么用除留余数法会产生冲突？

因为"取模"运算会把**一个很大的范围**映射到**一个很小的范围**。必然有多个不同的 `key` 得到相同的余数。

例如，设定 `p = 7`，哈希表长度为 `10`。

|键 key|哈希值 (key % 7)|存储位置|
|-|-|-|
|10|10 % 7 = **3**|位置 3|
|17|17 % 7 = **3**|位置 3 ← **冲突！**|
|24|24 % 7 = **3**|位置 3 ← **冲突！**|
|31|31 % 7 = **3**|位置 3 ← **冲突！**|

这四个不同的键，计算出的哈希值都是 `3`，所以它们都想被放到数组的 `3` 号位置。但一个位置只能放一个元素，于是冲突就发生了。

### 那如何优化呢？

需要尽量避免key % M中M的值为2的幂以及10的幂，尽量取不太接近2的整数幂的素数。c++也提供了**素数表**

### 为什么不能% 2 的幂

`a % 2^x` 在数学上的结果，就是 **a 除以 2^x 后剩下的余数**。

二进制除法中，除以 `1000...0`（即 2^x）就是**把被除数右移 x 位，余数就是被除数的低 x 位**。

例如，`13 % 8`：

- 13 的二进制：`1101`

- 8 的二进制：`1000`

- 13 除以 8，商 1，余数 5。5 的二进制是 `0101`。

- `1101` 的低 3 位正好是 `101`，即 5。

所以 `13 % 8 = 5`，正好等价于保留 13 的低 3 位。

![image.png](https://cdn2.flowus.cn/oss/1b196b3e-9969-4779-804e-b03b298746a8/image.png?time=1782974700&token=12f70d6b2d3ba493a91a16afdb00805f684d132cb4623244bac42a7e105f8d61&role=free)

### 在上述哈希表的建立中发现h(19)和h(30)位置是冲突的,下面提供几种解决方案

## 1.开放定址法

#### 核心思想：当位置出现冲突时，找一个没有存储数据的位置进行存储

#### 实现方法1：线性探测

#### 核心操作：

#### **插入**：计算 `hash(key)`，如果这个位置已被占用，就依次检查 `hash+1`、`hash+2`，直到遇到空位，插进去，若走到尾部也没找到空位，就从头开始找。

#### **查找**：同样从 `hash(key)` 开始找，如果该位置不是目标，则继续向后找；如果遇到空位，说明这个键不存在。

#### **删除**：通常不能直接清空，而是做一个"删除标记"（tombstone），避免中断查找链。

#### 线性探测的弊端：当有连续几个位置都冲突时，会出现群集/堆积的情况

### 代码实现：

```C++
//插入时线性探测，找到一个位置状态为delete或者empty的
    size_t start = HashFunc(key);
    size_t index = start;
    while(_tables[index]._state == exist)
    {
        index = (index + 1) % _tables.size();
        if(index == start)
            return false;
    }
//查找时线性探测
    while(_tables._state != empty)
    {
      
    }
```


## 基本框架

```C++
#include <iostream>
#include <vector>
using namespace std;

// 枚举：每个桶的状态
enum State {
    EMPTY,
    EXIST,
    DELETED
};

// 哈希表中每个节点的数据
template<class K, class V>
struct HashData {
    pair<K, V> _kv;
    State _state = EMPTY;
};

// 哈希表类（开放地址法，线性探测）
template<class K, class V>
class HashTable {
private:
    vector<HashData<K, V>> _tables;
    size_t _size = 0;  // 实际存储的元素个数

    // 哈希函数（除留余数法）
    size_t HashFunc(const K& key) {
        return (size_t)key % _tables.size();
    }

public:
    // 构造函数，初始化桶数量
    HashTable(size_t capacity = 10) {
        _tables.resize(capacity);
    }

    // 查找
    HashData<K, V>* Find(const K& key) {
        size_t start = HashFunc(key);
        size_t index = start;

        // 线性探测：一直找到空桶为止
        while (_tables[index]._state != EMPTY) {
            if (_tables[index]._state == EXIST && _tables[index]._kv.first == key) {
                return &_tables[index];  // 找到了,要返回指针&不能去掉
              //这里返回指针是因为后续插入和删除需要修改数据，而这个查找是辅助他们操作的，如果只是单独一个查找那不用返回指针也行
            }
            index = (index + 1) % _tables.size();  // 向后探测
            if (index == start) break;  // 已经绕了一圈，说明表满了
        }
        return nullptr;  // 没找到
    }

    // 插入
    bool Insert(const K& key, const V& val) {
        // 如果键已存在，则不允许插入重复键
        if (Find(key) != nullptr) {
            return false;
        }

        // 负载因子检查（超过0.7就扩容）
        if ((double)_size / _tables.size() >= 0.7) {
            Rehash();
        }

        size_t start = HashFunc(key);
        size_t index = start;

        // 线性探测：找到第一个状态为 EMPTY 或 DELETED 的位置
        while (_tables[index]._state == EXIST) {
            index = (index + 1) % _tables.size();
            if (index == start) {
                cout << "哈希表已满，插入失败" << endl;
                return false;
            }
        }

        // 插入键值对
        _tables[index]._kv = {key, val};
        _tables[index]._state = EXIST;
        _size++;
        return true;
    }

    // 扩容
    void Rehash() {
        vector<HashData<K, V>> oldTables = _tables;  // 保存旧表
        _tables.clear();
        _tables.resize(oldTables.size() * 2);  // 扩容为原来的2倍
        _size = 0;

        // 重新插入旧数据
        for (auto& data : oldTables) {
            if (data._state == EXIST) {
                Insert(data._kv.first, data._kv.second);
            }
        }
    }

    // 删除
    bool Erase(const K& key) {
        HashData<K, V>* target = Find(key);
        if (target == nullptr) {
            return false;  // 键不存在
        }

        // 标记为 DELETED，而不是真正移除
        target->_state = DELETED;//指针访问成员用"->",对象访问成员用"."
        _size--;
        return true;
    }
```


|返回方式|返回类型|能否表示"没找到"|能否修改原数据|
|-|-|-|-|
|按值返回|`HashData<k, v>`|❌ 不行|❌ 修改的是副本|
|返回指针|`HashData<k, v>*`|✅ 用 `nullptr`|✅ 可以修改|
|返回引用|`HashData<k, v>&`|❌ 不行（引用不能为空）|✅ 可以修改|

上面提到了用线性探测可能会出现群集/堆积的情况，所以下面引出"二次探测"

### 二次探测是什么

开放地址法中，发生冲突时，按下面的顺序找空位：

- 第 1 次探测：`(hash + 1²) % size`

- 第 2 次探测：`(hash + 2²) % size`

- 第 3 次探测：`(hash + 3²) % size`

- ...

通用公式：**`index = (hash + i²) % size`，i 从 1 开始递增，二次探测的步长是相对于"起始位置"计算的。**

---

### 和线性探测的区别

||**线性探测**|**二次探测**|
|-|-|-|
|探测步长|固定 1|`1², 2², 3², ...`|
|优点|简单，缓存友好|**不容易扎堆聚集**|
|缺点|容易产生"**聚集**"|可能跳过空位，**表大小必须是质数**|

---

### 如果让你手写二次探测，怎么改？

把插入函数里的**线性探测**部分：

```C++
index = (index + 1) % _tables.size();  // 线性探测
```


改成：

```C++
size_t i = 1;
index = (start + i * i) % _tables.size();
i++;
```


完整插入核心片段：

```C++
bool insert(const k& key, const v& value) {
    // ... 前面不变 ...
    
    size_t start = HashFunc(key);
    size_t index = start;
    size_t i = 1;

    while (_tables[index]._state == EXIST) {
        index = (start + i * i) % _tables.size();
        i++;
        if (i >= _tables.size()) return false;  // 防止死循环
    }

    // ... 后面插入代码不变 ...
}
```


查找和删除的逻辑也要同步改成二次探测步长，逻辑一样。

---

### 只需要记住这三点

1. **二次探测 = 冲突后按 `i²` 跳跃找空位**

2. **优点：不容易聚集；缺点：需要表大小是质数，可能浪费部分空间**

3. **代码上，把 `(index+1)` 改成 `(start + i*i)` 就行**

二次探测就是"换个步长"而已。面试时问到，只需要说清楚第二点对比和第三点思路就够了。

## 补充：java与c++中对于扩容中*2的优化方式

4. **Java翻倍求快，C++素数求稳**：

  - **Java**：容量保持2的幂次方，用**位运算**代替取模，速度快，且元素只需决定"原位"或"原位+旧容量"两个位置。

  - 为了性能和扩容效率，用位运算替代取模。

  - **C++（旧标准）**：容量从**素数表**中取下一个更大的素数，目的是让哈希值分布更均匀，减少冲突。

  - 为了减少哈希冲突，让元素分布更均匀。

## 2.链地址法

### **开放地址法是"找空位住"，链地址法是"同一层楼住不同房间"**

### 链地址法是什么？

### 链地址法 vs 开放地址法

当发生哈希冲突时，**不是去数组中找另一个空位，而是把冲突的多个元素串成一个链表，挂在同一个数组位置上。**

|对比|链地址法|开放地址法（线性探测）|
|-|-|-|
|**冲突解决**|挂在链表里|在数组中找下一个空位|
|**删除**|直接删除节点，简单|标记为 `DELETED`，不能直接清空|
|**查找**|先定位桶，再遍历链表|沿探测序列找，遇到 `EMPTY` 才停|
|**插入**|头插或尾插，不需要探测|需要线性或二次探测找空位|
|**表满问题**|永远不会"满"（链表可无限增长）|表满就真的满了（需要扩容）|
|**空间利用**|需要额外链表指针开销|更紧凑，但会产生 `DELETED` 碎片|
|**最坏情况**|退化成 O(n)（链表太长）|退化成 O(n)（探测序列太长）|

### 链地址法的核心代码

```C++
数组（桶）:
索引:  0    1    2    3    4
      +----+----+----+----+----+
      |    |    |    |    |    |
      +----+----+----+----+----+
                      ↓
                  [key1] → [key2] → [key3]  // 链表
```


```C++
// 每个桶是一个链表
template<class K, class V>
struct HashNode {
    pair<K, V> _kv;
    HashNode<K, V>* _next;
};

// 哈希表类
template<class K, class V>
class HashTable {
private:
    vector<HashNode<K, V>*> _tables;  // 指针数组
    size_t _size = 0;

    size_t HashFunc(const K& key) {
        return (size_t)key % _tables.size();
    }

public:
    HashTable(size_t capacity = 10) {
        _tables.resize(capacity, nullptr);
    }

    // 查找
    HashNode<K, V>* Find(const K& key) {
        size_t index = HashFunc(key);
        HashNode<K, V>* cur = _tables[index];
        while (cur) {
            if (cur->_kv.first == key) return cur;
            cur = cur->_next;
        }
        return nullptr;
    }

    // 插入（头插法）
    bool Insert(const K& key, const V& value) {
        if (Find(key)) return false;
        //计算桶的索引
        size_t index = HashFunc(key);
        //创建新节点头插挂在哈希桶上
        HashNode<K, V>* newNode = new HashNode<K, V>{ {key, value}, _tables[index] };
        _tables[index] = newNode;
        _size++;
        return true;
    }

    // 删除
    bool Erase(const K& key) {
        size_t index = HashFunc(key);
        //定位到桶并取出该桶链表第一个节点的指针
        HashNode<K, V>* cur = _tables[index];
        HashNode<K, V>* prev = nullptr;

        while (cur) {
            if (cur->_kv.first == key) //找到
            {
                if (prev) //不是表头的情况
                  prev->_next = cur->_next;
                else //是表头的情况
                  _tables[index] = cur->_next;
                delete cur;
                _size--;
                return true;
            }
          else//没找到
          {
            prev = cur;
            cur = cur->_next;
          }
        }
        return false;
    }
};
```


数组的每个位置（桶）不直接存元素，而是存一个**链表的头指针**。所有哈希到同一个位置的元素，都插入到这个链表中。

```C++
 vector<HashNode<K, V>*> _tables;  // 指针数组
```


这里存的是节点的指针

### 为什么存的是指针，而不是节点本身？

|存指针|存节点本身|
|-|-|
|`vector<HashNode<k,v>*>`|`vector<HashNode<k,v>>`|
|每个桶存一个**地址**（8字节）|每个桶存一整个**节点对象**|
|只存链表**头指针**|存的是元素本身|
|**动态**链接，链表可以无限增长|数组大小**固定**，不能动态增长|
|每个桶只需要一个指针的大小|每个桶需要一整个节点的大小，空间浪费|

### 为什么说链地址法更简单？

|对比点|开放地址法|链地址法|
|-|-|-|
|**删除逻辑**|标记 `DELETED`，需要特殊处理|直接删链表节点，没有负担|
|**插入逻辑**|沿着数组找空位，可能绕一圈|直接挂在链表头部或尾部|
|**状态管理**|需要 `EMPTY / EXIST / DELETED` 三个状态|只需要判断 `nullptr`|
|**探测逻辑**|线性 / 二次探测，要防止死循环|无探测，直接定位|
|**表满问题**|表满时需扩容|扩容阈值更宽松，链表可承载更多元素|